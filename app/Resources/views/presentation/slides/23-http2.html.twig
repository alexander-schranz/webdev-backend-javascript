{% extends 'presentation/slide.html.twig' %}

{% block title -%}
    Going crazy with http2
{%- endblock %}

{% block content %}
    <h3>HTTP2 Push Header</h3>

    <pre id="{{ register_component('code') }}" class="apache">
        {{- 'link: </js/main.js>; rel="preload"; as="script"'|e -}}
    </pre>

    <h3>Twig</h3>

    <pre id="{{ register_component('code') }}" class="twig">
        {{- '<script src="{{ preload(\'js/main.js\') }}"></script>'|e -}}
    </pre>
{% endblock %}
