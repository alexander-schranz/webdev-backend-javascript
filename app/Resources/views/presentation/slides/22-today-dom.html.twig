{% extends 'presentation/slide.html.twig' %}

{% block title -%}
    JS Part - What happens inside
{%- endblock %}

{% block content %}
    <pre id="{{ register_component('code') }}" class="js">
        {{- 'var componentRegistry = {};

function registerComponent(name, component) {
    componentRegistry[name] = component;
};

function startComponent(name, id, options) {
    var Component = componentRegistry[name];
    var instance = new Component();
    instance.initialize($(\'#\' + id), options);
}'|e -}}
    </pre>
{% endblock %}

{% block notes %}
    The registerComponent will just store the register component.
    The startComponents will interate over the given array of components
    which should be started and create a new Instance.
{% endblock %}
