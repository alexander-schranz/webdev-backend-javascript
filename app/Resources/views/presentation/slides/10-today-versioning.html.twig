{% extends 'presentation/slide.html.twig' %}

{% block title -%}
    Versioning with Twig and Symfony
{%- endblock %}

{% block content %}
    <pre id="{{ register_component('code') }}" class="twig">
        {{- '{# image "is other.js?v=123" #}' -}}<br>
        {{- '<img src="{{ asset(image) }}" alt="example">'|e -}}<br>
        {{- '<script src="{{ asset(\'main.js\', \'static\') }}"></script>'|e -}}
    </pre>

    <h3>Configuration</h3>

    <pre id="{{ register_component('code') }}" class="yml">
        {{- 'framework:'|e -}}<br>
        {{- '    assets:'|e -}}<br>
        {{- '        base_urls:'|e -}}<br>
        {{- '            - \'http://webdev.at/\''|e -}}<br>
        {{- '        packages:'|e -}}<br>
        {{- '            static:'|e -}}<br>
        {{- '                version: "1.0.1"'|e -}}<br>
        {{- '                version_format: "%%s?v=%%s"'|e -}}
    </pre>
{% endblock %}

{% block notes %}
    If you use the asset function to load your images from e.g. a cookiefree domain
    and they have there own versioning you can define own packages so the rule
    will only apply to this specific ones.
{% endblock %}
