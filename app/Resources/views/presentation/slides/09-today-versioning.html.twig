{% extends 'presentation/slide.html.twig' %}

{% block title -%}
    Versioning with Twig and Symfony
{%- endblock %}

{% block content %}
    <pre id="{{ register_component('code') }}" class="twig">
        {{- '<script src="{{ asset(\'main.js\') }}"></script>'|e -}}
    </pre>

    <h3>Configuration</h3>

    <pre id="{{ register_component('code') }}" class="yml">
        {{- 'framework:'|e -}}<br>
        {{- '    assets:'|e -}}<br>
        {{- '        version: "1.0.1"'|e -}}<br>
        {{- '        version_format: "%%s?v=%%s"'|e -}}
    </pre>

    <h3>Output:</h3>

    <pre id="{{ register_component('code') }}" class="twig">
        {{- '<script src="main.js?v=1.0.1"></script>'|e -}}
    </pre>
{% endblock %}

{% block notes %}#+
    In twig the asset function can be used to add a versioning
    to a specific file.

    The versioning format need to be configured in the framework bundle.
{% endblock %}
